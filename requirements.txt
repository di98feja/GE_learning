# GridEnforcer Integration Dependencies
# Core Home Assistant integration framework
homeassistant>=2024.1.0

# Communication protocols (from existing integration)
# NOTE: Some dependencies might be handled by Home Assistant core

# Data processing and validation
pydantic>=2.0.0
python-dateutil>=2.8.0

# Testing dependencies (for development)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Development tools
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0

# Additional dependencies that might be needed
voluptuous>=0.13.1  # For config validation (used in config_flow.py)